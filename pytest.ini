[pytest]
pythonpath = backend
testpaths = backend/tests
addopts = --strict-markers --import-mode=importlib
markers =
    unit: isolated tests without external services
    integration: tests spanning adapters or HTTP boundaries
    postgres: tests requiring TEST_DATABASE_URL
